# shopify-theme
## How to deploy
Requires an environment variable file with values for:
- DEV_PASSWD
- DEV_THEMEID
- DEV_SHOP

*used in config.yml*

##### Example Usage
```
> theme watch --vars=.env
```